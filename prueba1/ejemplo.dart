class primercomponente extends StatefulWidget {
  const primercomponente({Key? key}) : super(key: key);

  @override
  State<primercomponente> createState() => _primercomponenteState();
}

class _primercomponenteState extends State<primercomponente> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
